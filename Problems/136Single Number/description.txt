The problem:
Given an array of integers, every element appears twice except for one. Find that single one.

The solution:
The solution to this problem is pretty simple. Just do XOR operation for all integers in the given array, then, the element appears once will show up.
